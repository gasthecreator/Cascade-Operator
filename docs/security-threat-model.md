# Security threat model

**Date:** 2026-08-31 (PLAN.md §5 Phase 5)

This is a factual account of what this operator can read, what it can
write, and where its trust boundaries actually are — written the same way
this project's worklog entries are: state what was checked, not what
"should" be true. See [SECURITY.md](../SECURITY.md) for how to report a
vulnerability; this document is about design, not process.

## What the operator can do (RBAC)

The manager's `ClusterRole` (`config/rbac/role.yaml`, generated by
`controller-gen` from the `+kubebuilder:rbac` markers on
`internal/controller/cascadepolicy_controller.go`) grants exactly:

| API group | Resource | Verbs |
|---|---|---|
| `cascade.gideonsanni.dev` | `cascadepolicies` | create, delete, get, list, patch, update, watch |
| `cascade.gideonsanni.dev` | `cascadepolicies/finalizers` | update |
| `cascade.gideonsanni.dev` | `cascadepolicies/status` | get, patch, update |
| `networking.istio.io` | `destinationrules`, `virtualservices` | get, list, patch, update, watch |

Confirmed by reading the generated role directly, not assumed from the
markers: **no `create`/`delete` on `destinationrules`/`virtualservices`**
(the operator only ever patches objects that already exist — it never
creates or removes Istio config), **no core-resource verbs** (no `pods`,
`services`, `secrets`, `configmaps`), and **no wildcard groups or
resources**. This is the actual, current RBAC surface, not an aspiration —
verified against the checked-in generated manifest, the same one `make
verify-generate` gates against drift.

**What this means concretely**: a compromised operator pod could read and
modify any `CascadePolicy`, `DestinationRule`, or `VirtualService` in any
namespace it's granted access to (the `ClusterRole` is cluster-scoped, per
kubebuilder's default scaffold — see "Known gaps" below), but cannot read
`Secret`s, create new workloads, or touch anything outside those three
resource types.

## Trust boundaries

1. **The `CascadePolicy` CRD is the only user-facing input surface.**
   Everything the operator does — which Istio objects to resolve, what
   thresholds to detect against, whether to mitigate or only observe — is
   driven by this one CRD's spec. It is validated twice: the CRD's own
   OpenAPI schema (`+kubebuilder:validation` markers — minimums, maximums,
   required fields, enums) enforced by the API server before the object is
   even persisted, and the validating admission webhook
   (`internal/webhook/v1alpha1/cascadepolicy_webhook.go`, PLAN.md §5 Phase 3)
   for cross-field/semantic checks the schema can't express (self-dependency,
   duplicate `dependsOn` entries, malformed Service FQDNs).
2. **Prometheus is a read-only dependency.** The operator only ever issues
   PromQL `Query` calls (`internal/metrics/client.go`) — it never writes
   to Prometheus. A compromised or malicious Prometheus endpoint could feed
   the operator false metrics and cause spurious trips/mitigations
   (a denial-of-service against the *mesh*, via the operator acting on bad
   data), but cannot use the operator as a path to compromise anything
   else — the blast radius of a bad Prometheus response is bounded by
   "the operator patches Istio objects it already had permission to patch
   anyway."
3. **The notify webhook URL (`--notify-webhook-url` / `NOTIFY_WEBHOOK_URL`,
   PLAN.md §5 Phase 4) is operator-level configuration, not
   CascadePolicy-level.** It cannot be set or influenced by a
   `CascadePolicy` object itself — only by whoever configures the
   Deployment. This is deliberate: allowing a namespaced CR to specify an
   arbitrary outbound webhook URL would let anyone able to create a
   `CascadePolicy` in their own namespace direct the operator's cluster
   identity to POST trip/restore event data (dependency hostnames,
   confidence scores, evidence strings — no secrets) to an
   attacker-controlled endpoint. Keeping this operator-scoped avoids that
   entirely.
4. **The admission webhook is the only network-facing surface that accepts
   inbound requests from inside the cluster** (the API server calls it on
   every `CascadePolicy` create/update). It runs over TLS
   (`cmd/main.go`'s `--webhook-cert-path` wiring, `config/certmanager/` for
   production cert issuance) and only ever validates — it has no mutating
   path, so a bug in it can reject a legitimate write but cannot itself
   inject unauthorized changes to a `CascadePolicy`.
5. **The metrics endpoint defaults to HTTPS with authn/authz**
   (`--metrics-secure=true` is the default in `cmd/main.go`, using
   controller-runtime's `filters.WithAuthenticationAndAuthorization` —
   only service accounts the cluster's RBAC grants `nonResourceURLs`
   access to `/metrics` can actually read it). It is disabled to plain HTTP
   only when explicitly requested (`--metrics-secure=false`), which this
   project's own live-verification work has done for local, out-of-cluster
   `go run` sessions against the dev Kind cluster — never for anything
   deployed.
6. **The operator's own dev deployment holds a real Linkerd mTLS identity**
   (`cascade-operator-controller-manager.cascade-operator-system.serviceaccount.identity.linkerd.cluster.local`,
   `hack/deploy-operator.sh`'s Linkerd mesh-injection step), needed so it
   can query `linkerd-viz`'s Prometheus at all — that Prometheus is locked
   down by a deny-by-default `AuthorizationPolicy` on its query port,
   confirmed live: an unmeshed caller gets a clean HTTP 403 regardless of
   RBAC or query correctness. The operator is granted access via a
   *second*, additive `AuthorizationPolicy`
   (`linkerd-viz/prometheus-admin-cascade-operator`) targeting the same
   `Server` as `linkerd-viz`'s own pre-existing `metrics-api` grant —
   Linkerd's own policy validator webhook rejects more than one
   ServiceAccount per `AuthorizationPolicy`, so this is deliberately a
   second policy, not an edit to the existing one, and it grants exactly
   one capability (read that one Prometheus's query API) to exactly one
   identity. Meshing the operator was confirmed live not to newly restrict
   its *other* inbound traffic before doing it: this cluster's
   `defaultInboundPolicy` is `all-unauthenticated`, so istio-system's own
   unmeshed Prometheus scraping the operator's `/metrics` (trust boundary 5
   above) keeps working exactly as before.

## Known gaps (stated plainly, not hidden)

- **The `ClusterRole` is cluster-scoped**, per kubebuilder's default
  scaffold — the operator's RBAC grants apply cluster-wide, not restricted
  to specific namespaces. For this project's actual demo/dev use (a single
  namespace, `default`), this is broader than strictly necessary. Narrowing
  it to a `Role`+`RoleBinding` per watched namespace would be the
  production-hardening step here; not done in this pass since it would
  need a real multi-namespace deployment story to validate against, and
  this project's dev cluster only ever uses one namespace.
- **Resolved**: the admission webhook is now deployed to the persistent dev
  Kind cluster (`hack/deploy-operator.sh`, cert-manager for real cert
  issuance) — Phase 3's own follow-up, previously stated here as an open
  gap since only `envtest`'s ephemeral test certs had exercised the TLS
  wiring. Still worth naming what wasn't specifically re-verified in that
  slice: the webhook's own rejection behavior (self-dependency, duplicate
  `dependsOn`, malformed FQDNs) was proven correct via `envtest` and not
  re-exercised against the cert-manager-issued cert path specifically —
  the TLS *serving* path is now real, the validation *logic* re-check
  against it is not a new test, just the same one running on real
  infrastructure.
- **No `NetworkPolicy` restricting the operator pod's egress.** It can, in
  principle, reach any address in the cluster (or beyond, if the cluster's
  network allows it) — including wherever `--prometheus-url` or
  `--notify-webhook-url` point. Both of those are operator-level
  configuration (see Trust boundaries above), not attacker-influenceable
  via the CRD, which is the main mitigation currently in place; a
  `NetworkPolicy` scoping egress to just Prometheus and the configured
  notify endpoint would be a real additional layer, not yet added.
- **No image signing or provenance verification** on the manager image
  itself (`Dockerfile`/`config/manager/manager.yaml`'s `image:
  controller:latest`) — standard for a portfolio-scale project, would be
  a real gap for anything running in a genuinely untrusted environment.

## What's intentionally out of scope for this document

Threats from a fully compromised Kubernetes control plane, a malicious
cluster-admin, or supply-chain compromise of this project's own
dependencies (`go.mod`/`go.sum`) — the latter is what `govulncheck` and
Dependabot (PLAN.md §5 Phase 1) already continuously check for, not
something a static document adds value restating.
